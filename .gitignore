# OS / editor
.DS_Store

# Secrets
.env

# Python
__pycache__
.pytest_cache/

# Virtualenv / vector store
.venv
vectordb

# Vendored third-party tooling (kept local, not committed)
# Anchored to repo root so it never matches nested source dirs.
/tools/

# Competition data dumps & archives (kept local, not committed)
# Root-anchored: avoids case-insensitive collisions with src/algorithms/round_*/
*.zip
/267453/
/data/competition_rounds/
/backtests/

# Generated logs
*.log
